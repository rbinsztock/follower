Exemple Application To use Acts_as_Follower into Rails 3 and Ruby 1.9.3.

- install RoR (RVM)
- clone the project : git clone url
- rake db:seed to populate your base, look into the seed.rb for user and password.
